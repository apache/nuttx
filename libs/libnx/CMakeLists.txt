if (CONFIG_NX)
  nuttx_add_library(nx)

  add_subdirectory(bin)
  add_subdirectory(kbin)
  add_subdirectory(nx)
  add_subdirectory(nxfonts)
  add_subdirectory(nxglib)
  add_subdirectory(nxmu)
  add_subdirectory(nxtk)
endif()
